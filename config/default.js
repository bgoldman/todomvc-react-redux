// not using `export default` because the config module can't read it that way
module.exports = {
    "publicConfigs": ["client"],
    "client": {
        "environment": null,
        "port":        null,
    },
}
